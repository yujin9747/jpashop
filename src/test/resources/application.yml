spring:
#  아래의 설정이 모두 없어도, spring은 다른 디비 연결이 없으면 기본적으로 메모리 모드로 테스트를 진행한다.
#  datasource:
#    url: jdbc:h2:mem:test  -> h2를 메모리 모드로 테스트 하고 싶을 때
#    hikari:
#      username: sa
#      password:
#      driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create-drop -> 기본적으로 스프링은 이 방식으로 진행한다. 만든 모든 테이블을 삭제해서 자원정리.
#    properties:
#      hibernate:
#        format_sql: true
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace


